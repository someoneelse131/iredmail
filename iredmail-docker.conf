# iRedMail Docker Configuration
# IMPORTANT: Do not use whitespace, tabs, or quotes in values!

# =============================================================================
# REQUIRED SETTINGS - You MUST configure these
# =============================================================================

# Mail server hostname (must match DNS A record)
HOSTNAME=mail.kirby.rocks

# Your primary mail domain
FIRST_MAIL_DOMAIN=kirby.rocks

# Admin password for postmaster@kirby.rocks
# CHANGE THIS to a strong password!
FIRST_MAIL_DOMAIN_ADMIN_PASSWORD=CHANGE_ME_TO_A_SECURE_PASSWORD

# =============================================================================
# AUTO-GENERATED TOKENS - These are pre-generated, but you can regenerate
# =============================================================================

# API token for mlmmjadmin (mailing list manager)
MLMMJADMIN_API_TOKEN=gMwH2UcRMX7eOhTYgNbDn1m30rW2u5pU5XKFRzDopb0=

# DES key for Roundcube webmail session encryption
ROUNDCUBE_DES_KEY=Aq1BhxNSD4XUZfIg3Vij9o2mp7iPqcnD

# =============================================================================
# DATABASE PASSWORDS - Set these to prevent random regeneration on restart
# =============================================================================

# MySQL root password
MYSQL_ROOT_PASSWORD=CHANGE_ME_mysql_root_password

# Passwords for various service accounts (set to prevent random changes)
VMAIL_DB_BIND_PASSWORD=CHANGE_ME_vmail_password
VMAIL_DB_ADMIN_PASSWORD=CHANGE_ME_vmail_admin_password
AMAVISD_DB_PASSWORD=CHANGE_ME_amavisd_password
IREDADMIN_DB_PASSWORD=CHANGE_ME_iredadmin_password
IREDAPD_DB_PASSWORD=CHANGE_ME_iredapd_password
ROUNDCUBE_DB_PASSWORD=CHANGE_ME_roundcube_password
SOGO_DB_PASSWORD=CHANGE_ME_sogo_password
SOGO_SIEVE_MASTER_PASSWORD=CHANGE_ME_sogo_sieve_password
FAIL2BAN_DB_PASSWORD=CHANGE_ME_fail2ban_password

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Timezone (default: UTC)
# TZ=America/New_York

# Disable ClamAV to save resources (not recommended for production)
# DISABLE_CLAMAV=YES

# Disable SpamAssassin (not recommended)
# DISABLE_SPAMASSASSIN=YES
